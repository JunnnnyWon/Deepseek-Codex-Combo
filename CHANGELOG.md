# Changelog

## 0.1.0

- Added the DeepSeek-Codex-Combo CLI, provider proxy, installer, plugin bundle, rules, hooks, LSP, AST-grep, hashline, and orchestration flows.
- Added project-memory generation with `dcc init-deep`.
- Added release packaging manifests and checksum generation for the local bundle.
